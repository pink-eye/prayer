export const BASE_URL = 'https://prayer.herokuapp.com'

export const API_PATHS = {
	COLUMNS: `${BASE_URL}/columns`,
	PRAYERS: `${BASE_URL}/prayers`,
	AUTH: `${BASE_URL}/auth`,
	COMMENTS: `${BASE_URL}/comments`,
}
